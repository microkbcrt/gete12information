<iframe 
    src="https://e.weather.com.cn/mweather/101280101.shtml" 
    width="400px" 
    height="750px" 
    frameborder="0" 
    scrolling="auto"
    title="广州天气信息"
></iframe>

OpenClaw，中文昵称“小龙虾”。自2026年初发布以来迅速走红，因其强大的自动化能力被广泛应用于个人办公、开发辅助和团队协作中，真正实现了“用自然语言替代人工完成重复性工作”

<script>
(function() {
    // 创建警告弹窗样式
    var style = document.createElement('style');
    style.innerHTML = `
        .warning-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            animation: fadeIn 0.3s ease;
        }
        .warning-popup {
            background: linear-gradient(135deg, #ff0000, #cc0000);
            padding: 40px 60px;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(255, 0, 0, 0.6), 0 0 30px rgba(255, 255, 0, 0.8);
            text-align: center;
            max-width: 80%;
            min-width: 300px;
            border: 5px solid #ffcc00;
            color: white;
            font-family: Arial, sans-serif;
            position: relative;
            animation: pulse 1.5s infinite alternate;
        }
        .warning-popup h2 {
            font-size: 2.5em;
            margin: 0 0 15px 0;
            text-shadow: 2px 2px 4px #000;
        }
        .warning-popup p {
            font-size: 1.8em;
            margin: 0;
            line-height: 1.5;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes pulse {
            from { box-shadow: 0 10px 40px rgba(255, 0, 0, 0.6), 0 0 30px rgba(255, 255, 0, 0.8); }
            to { box-shadow: 0 10px 50px rgba(255, 0, 0, 0.9), 0 0 50px rgba(255, 255, 0, 1); }
        }
    `;
    document.head.appendChild(style);

    // 创建弹窗HTML
    var overlay = document.createElement('div');
    overlay.className = 'warning-overlay';
    overlay.innerHTML = `
        <div class="warning-popup">
            <h2>⚠️ 警告</h2>
            <p>文瑞雪正在用一体机摄像头窥视教室情况！</p>
        </div>
    `;
    document.body.appendChild(overlay);

    // 10秒后自动关闭弹窗
    setTimeout(function() {
        if (overlay && overlay.parentNode) {
            overlay.parentNode.removeChild(overlay);
        }
    }, 10000); // 10000毫秒 = 10秒
})();
</script>
